{
  "name": "backend",
  "version": "1.0.0",
  "description": "cat dashboard to track my cats weight, when we are running low on supplies, and to keep up with chores for our cats",
  "license": "ISC",
  "author": "Emil",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch --env-file=.env.dev server.js"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "globals": "^17.7.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-validator": "^7.3.2",
    "mongoose": "^9.7.3",
    "multer": "^2.2.0"
  }
}
